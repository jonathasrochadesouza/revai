{
  "name": "revai-web",
  "version": "3.0.0-alpha.0",
  "private": true,
  "description": "RevAI Platform web app — Paper Light design system",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "node scripts/next-port.mjs dev",
    "build": "next build",
    "start": "node scripts/next-port.mjs start",
    "lint": "eslint src next.config.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "audit:prod": "npm audit --omit=dev"
  },
  "dependencies": {
    "lucide-react": "^1.27.0",
    "next": "16.2.12",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitest/coverage-v8": "^4.1.11",
    "eslint": "^9.39.0",
    "eslint-config-next": "16.2.12",
    "jsdom": "^28.1.0",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^4.0.18"
  },
  "//overrides": "Hand-pinned patched transitives. `npm audit fix --force` would 'fix' these by downgrading Next.js to 9.3.3 — far worse than the advisories it resolves. See SECURITY-NOTES.md for the brace-expansion situation.",
  "overrides": {
    "nanoid": "^3.3.17",
    "postcss": "^8.5.18",
    "sharp": "^0.35.0"
  }
}
