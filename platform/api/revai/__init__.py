"""RevAI Platform — AI code review backend.

Local-first, single-user. Runs on 127.0.0.1 and stores everything as plain YAML
under ``~/.revai/`` so the data stays readable, diffable and yours.
"""

__version__ = "3.0.0a0"
